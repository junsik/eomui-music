# 변경 이력

## 1.4.0

첫 정식 구성. 설치 프로그램과 자동 정리가 들어간 버전입니다.

### 추가

- **유튜브 열기** — 음악 목록 화면 위쪽의 큰 빨간 버튼과 트레이 메뉴에서
  YouTube를 바로 열 수 있음. 어머니가 주소를 입력하실 필요가 없음
- **음악 목록 화면** — 바탕화면 아이콘을 누르면 받은 곡이 큰 글씨로 한 줄씩 뜨고
  **듣기** 버튼으로 바로 재생. 보관함에 내려간 곡도 여기서는 똑같이 보이므로
  폴더를 찾아 들어갈 필요가 없음. 20곡씩 나눠 보여 수백 곡이어도 첫 화면이 짧음.
  화면 배율 설정을 그대로 따라감
- **설치 프로그램** (Inno Setup) — `%LOCALAPPDATA%\Programs\어무이음악\`에
  관리자 권한 없이 설치. 제어판 제거 항목 등록
- **필수 파일 번들** — `yt-dlp` · `ffmpeg` · `deno`를 설치 파일에 포함.
  첫 실행부터 바로 동작 (예전에는 약 170MB를 내려받아야 했고, ffmpeg 배포처가
  느려 실패하는 경우가 있었음)
- **자동 실행** — 로그인 시 자동 시작 (HKCU Run 키). 트레이 메뉴에서 켜고 끄기
- **중복 실행 방지** — 이름 있는 뮤텍스. 설치 프로그램도 같은 이름을 확인
- **바탕화면 음악 자동 정리** — 최근 30일 이내 · 최대 12곡만 바탕화면에 두고
  나머지는 `음악 보관함\2026년 07월\` 형태로 이동. 기준은 `settings.json`으로 조정
- **기존 MP3 일괄 정리** — 트레이 메뉴에서 실행. 확인 창을 거침
- **같은 영상 중복 다운로드 방지** — 다시 받지 않고 안내
- **yt-dlp 자동 갱신** — 주 1회 `yt-dlp -U`. YouTube 변경으로 멈추는 것을 방지
- **콘솔 출력** — 터미널에서 실행하면 로그가 바로 보임 (`-console`로 창 생성)
- **필수 파일 자동 다운로드** — 번들이 없는 경우에도 실행 시 알아서 받음.
  진행률은 트레이 툴팁과 상태 페이지에 표시

### 수정

- **다운로드할 때 검은 콘솔 창이 깜빡이던 문제** — 이 프로그램은 GUI 서브시스템이라
  자기 콘솔이 없어서, yt-dlp·ffmpeg 같은 콘솔 프로그램을 실행하면 Windows가
  새 콘솔 창을 만들어 줬음. `CREATE_NO_WINDOW`로 창 없이 실행하도록 수정
- **`--js-runtimes` 인자 형식 오류** — `RUNTIME[:PATH]` 형식인데 경로만 넘겨
  yt-dlp가 드라이브 문자(`C:`)를 런타임 이름으로 오인하고 무시했음.
  JS 런타임 탐지가 사실상 동작하지 않던 문제
- **한국어 파일명 깨짐** — 파일명을 바이트 단위로 자르던 것을 글자 단위로 변경.
  33자가 넘는 한글 제목이 글자 중간에서 잘렸음
- **CP949 디코더가 동작하지 않던 문제** — Windows에 없는 `iconv`를 호출했고,
  실패해도 오류를 반환하지 않아 깨진 문자열이 그대로 통과했음.
  `golang.org/x/text/encoding/korean`으로 교체
- **포트 바인딩 실패가 조용히 묻히던 문제** — 서버가 없는데 트레이는
  "준비 완료"로 표시됐음. 이제 원인이 툴팁에 고정 표시됨
- **오래된 임시 파일 정리가 진행 중인 다운로드를 삭제하던 문제**
- **yt-dlp 실행 실패 시 로그에 단서가 남지 않던 문제** — 실행 파일이 없으면
  stderr가 비어 `yt-dlp error:` 만 기록됐음
- **Windows 백신이 방금 쓴 실행 파일을 잠가 설치가 간헐 실패하던 문제** — 재시도 추가
- **경로 대소문자 차이로 제자리 파일의 이름이 바뀌던 문제** — Windows 파일 시스템은
  대소문자를 구분하지 않는데 문자열로만 비교해, `desktop`과 `Desktop`을 다른 곳으로
  보고 옮기려다 이름이 겹쳐 `노래 (2).mp3`가 되었음

### 보안

- **서버를 `127.0.0.1`에만 바인딩** — 예전에는 `0.0.0.0`이라 같은 공유기의
  다른 기기가 바탕화면에 파일을 쓸 수 있었음
- **`Access-Control-Allow-Origin: *` 제거** — YouTube 계열과 확장 출처만 허용
- **CSRF 방어** — `/api/download`는 `X-Eomui-Client` 헤더 또는 허용된 `Origin`이
  있어야 동작. 다른 웹사이트가 `<img>` 태그로 다운로드를 거는 것을 차단
- **오류 응답에서 내부 경로 제거** — yt-dlp stderr가 그대로 노출되던 것을 차단

### 안정성

- yt-dlp 실행에 30분 타임아웃 추가 (긴 영상 지원은 유지)
- 동시 다운로드 2개로 제한. 초과 시 안내
- 다운로드 실패 시 임시 파일 정리

### 라이선스

- 저장소를 공개로 전환하면서 **MIT 라이선스**로 변경 (기존 "All rights reserved")
- **FFmpeg GPL v3 소스 제공 고지 추가** — 공개 배포가 되면서 재배포 의무가
  실제로 발생. 설치 폴더에 `ffmpeg-source-offer.txt`를 함께 넣고,
  상위 소스·빌드 스크립트 경로와 3년 서면 제공 고지를 명시
- 앱 자체는 FFmpeg와 링크하지 않고 별도 프로세스로 실행하므로 GPL 적용 대상이 아님

### 그 밖

- 설치 마지막 화면에서 **크롬 확장 등록 안내(HTML)** 와 확장 폴더를 함께 열어 줌.
  안내 페이지는 자기 위치에서 확장 폴더 경로를 계산해 보여 주므로 설치 경로와 무관하게 정확
- 프로젝트 폴더 재구성 — `standalone-go/` → `app/`, 문서를 `docs/`로 분리
- 확장 버전 1.3.0 → 1.4.0 (서버가 헤더를 확인하므로 함께 갱신 필요)

---

## 이전 작업

1.4.0 이전의 상세한 작업 기록은 [docs/worklog.md](docs/worklog.md)에 있습니다.
